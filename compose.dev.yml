services:
  app_db:
    image: 'postgres:16-alpine'
    environment:
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: app_password
      POSTGRES_DB: app_db
    ports:
      - 5432:5432
    networks:
      - app_network
    restart: 'no'

networks:
  app_network:
    driver: bridge
